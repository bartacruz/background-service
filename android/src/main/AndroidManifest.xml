<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lagruva.backgroundservice">
    <application>
        <service android:name="com.lagruva.backgroundservice.MessagingService" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="com.lagruva.backgroundservice.BackgroundService"
            android:foregroundServiceType="location">
    <!-- Any inner elements would go here. -->
        </service>
        <service android:name="com.lagruva.backgroundservice.GeolocationService" android:foregroundServiceType="location"
        android:enabled="true"
        android:exported="true" ></service>
    </application>
</manifest>
